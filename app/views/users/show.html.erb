<% provide(:title, "#{@user.name}") %>
<div class="row">
  <aside class="col-md-2">
    <section class="user_info">
      <%= avatar_for @user, size: 80 %>
      <h2>
        <%= @user.name %>
      </h2>

      <nav id="sidebar">
        <ul class="list-unstyled components">
          <li>
            <a href="#">About Me</a>
          </li>
          <li> 
            <a href="#">My Plan</a>
          </li>
          <li>
            <a href="#">Log it</a>
          </li>
    </section>
  </aside>
  <div class="col-md-10">
    <h1><%= @user.name %> | <%= @user.email %> </h1>
    <hr />
    <h1><%= @user.name if logged_in? %>
      <hr />
    <h1> You personal plan and log will begin here </h1>
  </div>
</div>